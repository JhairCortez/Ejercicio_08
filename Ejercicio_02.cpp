#include<iostream>
using namespace std;
{
void mostrarTabla(int num)
    for(int i = 1; i <= 10; i++)
        cout << num << " x " << i << " = " << num * i << endl
}

void mostrarTablasDesde2(int limite)
{
    for(int i = 2; i <= limite; i++)
        mostrarTabla(i)
        cout << end;
}

int main()
{
    int n
    cout << "Ingrese un número: "
    cin >> n
    mostrarTablasDesde2(n)
}
